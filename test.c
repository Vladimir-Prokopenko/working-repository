#include <stdio.h>
#include <math.h>
#include <stdbool.h>
int x;

int main()

{
    // system("chcp 65001");
    int a = 5;
    if (++a > 5 && ++a > 6 && a = 0)
    {
        printf("Умова істина\n");
    }
    else
    {
        printf("Умова хибна\n");
    }
    printf("Кінцеве значення a: %d\n", a);

    return 0;
}
