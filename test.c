#include <stdio.h>
#include <math.h>
#include <stdbool.h>

#define SQUARE(x) (x * x)

int main()

{
    // system("chcp 65001");
    // int hundreds = (num / 256^2) % 256;
    // int tens = (num / 256^1) % 256;
    // int ones = (num / 256^0) % 256;

    // printf("Сума цифр: %d\n", hundreds + tens + ones);

    return 0;
}
